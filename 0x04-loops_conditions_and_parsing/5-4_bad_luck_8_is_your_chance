#!/usr/bin/env bash
# displays bad luck for the 4th loop iteration etc.
x=1
while [ $x -le 10 ]
do
	echo "Best School"
  x=$((x + 1))
  if x=$(( x > 3));
       then echo "bad luck"
  elif x=$(( x > 7)); 
     then echo "good luck"
   
  fi 
done
